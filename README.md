## Code Challenge

- Each problem lives under `src/problem1`, `src/problem2`, `src/problem3`.
- Every problem folder has its own `document.md`.
- Please read that `document.md` to learn how to run the project and what changes were made for that specific problem.

